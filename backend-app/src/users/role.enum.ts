/**
 * Enum representing the available roles for users.
 * Used for role-based access control (RBAC).
 */
export enum Role {
  Admin = 'admin', // Full access
  User = 'user',   // Restricted access
}
